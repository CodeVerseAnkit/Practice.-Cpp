#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
    double num = 45.0;
    cout << "Normal: " << num << endl;
    cout << "with showpoint: " << showpoint << num << endl;
    cout << "with noshowpoint: " << noshowpoint << num << endl;

    return 0;
}